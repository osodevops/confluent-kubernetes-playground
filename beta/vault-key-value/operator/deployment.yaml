apiVersion: apps/v1
kind: Deployment
metadata:
  name: confluent-operator
spec:
  template:
    spec:
      containers:
        - args:
            - --debug=false
            - --namespaces=sandbox
          name: confluent-rbac-operator
