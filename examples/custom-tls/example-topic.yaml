apiVersion: platform.confluent.io/v1beta1
kind: KafkaTopic
metadata:
  name: foobar
spec:
  kafkaClusterRef:
    name: kafka
    namespace: custom-tls
  kafkaRest:
    authentication:
      type: bearer
      bearer:
        secretRef: mds-client
    endpoint: https://kafka.custom-tls.svc.cluster.local:8090
