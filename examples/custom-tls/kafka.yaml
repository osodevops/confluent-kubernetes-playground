---
apiVersion: platform.confluent.io/v1beta1
kind: Kafka
metadata:
  name: kafka
spec:
  tls:
    secretRef: tls-group1
  dependencies:
    zookeeper:
      endpoint: zookeeper.custom-tls.svc.cluster.local:2182
  metricReporter:
    bootstrapEndpoint: kafka.custom-tls.svc.cluster.local:9071
