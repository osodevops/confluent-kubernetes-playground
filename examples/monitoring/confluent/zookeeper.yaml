apiVersion: platform.confluent-rbac.io/v1beta1
kind: Zookeeper
metadata:
  name: zookeeper
spec:
  authentication:
    type: digest
    jaasConfig:
      secretRef: credential
  tls:
    autoGeneratedCerts: true
