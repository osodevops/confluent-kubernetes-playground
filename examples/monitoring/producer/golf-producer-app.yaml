---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: golf-console-producer
  namespace: sandbox
spec:
  serviceName: golf-console-producer
  podManagementPolicy: Parallel
  replicas: 1
  selector:
    matchLabels:
      app: golf-console-producer
  template:
    metadata:
      labels:
        app: golf-console-producer
    spec:
      containers:
        - name: golf-console-producer
          image: confluentinc/cp-kafka:latest
          command:
            - /bin/sh
            - -c
            - |
              kafka-producer-perf-test \
                --topic golf-topic  \
                --record-size 1024 \
                --throughput 500 \
                --producer.config /mnt/kafka.properties \
                --num-records 100000
          volumeMounts:
            - name: kafka-properties
              mountPath: /mnt
            - name: kafka-ssl-autogenerated
              mountPath: /mnt/sslcerts
          resources:
            requests:
              memory: 512Mi # 768Mi
              cpu: 500m # 1000m
      volumes:
        - name: kafka-properties # Create secret with name `kafka-client-config` with client configurations
          secret:
            secretName: kafka-client-config
---
apiVersion: v1
kind: Service
metadata:
  name: golf-console-producer
  namespace: sandbox
spec:
  clusterIP: None