apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: venafi-secret
spec:
  backendType: secretsManager
  data:
  - key: egsp_venafi_service_account
    name: username
    property: username
  - key: egsp_venafi_service_account
    name: password
    property: password