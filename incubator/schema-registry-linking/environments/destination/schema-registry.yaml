apiVersion: platform.confluent.io/v1beta1
kind: SchemaRegistry
metadata:
  name: schemaregistry
spec:
  passwordEncoder:
    secretRef: password-encoder-secret
  enableSchemaExporter: true
  externalAccess:
    type: loadBalancer
    loadBalancer:
      domain: oso-destination