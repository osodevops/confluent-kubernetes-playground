---
apiVersion: platform.confluent.io/v1beta1
kind: Kafka
metadata:
  name: kafka
spec:
  tls:
    secretRef: tls-kafka
  listeners:
    internal:
      authentication:
        type: mtls
      tls:
        enabled: true
        # Will use the certificates in the parent secretRef tls-kafka
