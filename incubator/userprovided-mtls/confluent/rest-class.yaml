apiVersion: platform.confluent.io/v1beta1
kind: KafkaRestClass
metadata:
  name: default
spec:
  kafkaRest:
    authentication:
      type: mtls
    tls:
      directoryPathInContainer: /mnt/sslcerts/
      secretRef: tls-kafka

