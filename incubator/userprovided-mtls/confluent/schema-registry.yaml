apiVersion: platform.confluent.io/v1beta1
kind: SchemaRegistry
metadata:
  name: schemaregistry
spec:
  tls:
    autoGeneratedCerts: false
    secretRef: tls-schemaregistry
  dependencies:
    kafka:
      authentication:
        jaasConfig: null
        type: mtls