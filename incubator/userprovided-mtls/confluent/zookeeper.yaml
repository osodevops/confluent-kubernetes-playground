apiVersion: platform.confluent.io/v1beta1
kind: Zookeeper
metadata:
  name: zookeeper
spec:
  authentication:
    type: mtls
    principalMappingRules:
      - RULE:CN=([^,]*).*
  tls:
    secretRef: tls-zookeeper