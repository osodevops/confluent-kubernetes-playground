apiVersion: v1
kind: Pod
metadata:
  name: kafkacat
  namespace: tools
  labels:
    role: kafkacat
spec:
  containers:
    - name: kafkacat
      image: confluentinc/cp-kafkacat
      command:
        - sh
        - -c
        - "tail -f /dev/null"