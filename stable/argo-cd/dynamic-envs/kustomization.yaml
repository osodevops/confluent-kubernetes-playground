apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  - confluent-dev-on-feature-branch.yaml

secretGenerator:
  - name: github-token
    files:
      - token=github-token.txt
    options:
      disableNameSuffixHash: true